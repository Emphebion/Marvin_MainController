Read up on the Spark class and all relevant functions in Table.py.
Then check the recent fixes to the Serial connection from Main controller to Arduino Mega.

The issue is the following: due to an unintended bug/feature in the code. The sparks appeared as random flashed on the table, chaotic in both location, color, length and time.

The simulation still shows the correct effect, however the table no longer does. I want you to investigate and predict the effect the current code would generate on the table.

Your output is the result of your investigation and plan for improvements needed in /docs/plans in the Marvin Main Controller local repo